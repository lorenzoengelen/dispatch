var db = require('../db');

exports.create = function(request, cb) {
  // var request = {
  //   id: id,
  //   timestamp: time,
  //   longitude: longitude,
  //   latitude: latitude
  // };

  // db.save(request, cb);
  cb('no error');
};

exports.update = function(id, info, cb) {

};